import { BackgrounblockDirective } from './backgrounblock.directive';

describe('BackgrounblockDirective', () => {
  it('should create an instance', () => {
    // const directive = new BackgrounblockDirective( , null);
    // expect(directive).toBeTruthy();
  });
});
